<p id="notice"><%= notice %></p>

<div class="col l6"><h1>Mood Modes Dictionary</h1></div>

<div class="container">

  <div class="row">
   <% @moods.each do |mood| %>
   <% if mood.image_url %>

<div class="col s12 m6 l3">
          <div class="card">
            <div class="card-image">
              <img src="<%= mood.image_url %>">
              <span class="card-title"><%= mood.name %></span>
            </div>
            <div class="card-content">
              <p></p>

            </div>
            <div class="card-action">
              <%= link_to 'I Feel Like This', mood %></a>
            </div>
          </div>
        </div>
    <% end %>
   <% end %>
</div>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @moods.each do |mood| %>
      <tr>
        <td><%= mood.name %></td>
        <td><%= mood.description %></td>
        <td><%= mood.image_url %></td>
        <td><%= link_to 'Show', mood %></td>
        <td><%= link_to 'Edit', edit_mood_path(mood) %></td>
        <td><%= link_to 'Destroy', mood, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mood', new_mood_path %>
